# E2E run · 2026-09-19 18:23 · cloud-browser

- **Agent:** Cursor cloud (`bc-5953c0be-6137-44d7-99ec-3970d180a90f`)
- **Prompt:** Deploy the project and test the scenarios in `tests/e2e` in the browser
- **Git:** anakata-api `a377a5e` · panel `d814652` · engine `d394294` · ui `8520139`
- **Mode:** preview
- **Stack:** `status.sh` ALL UP (after a container-only storage permission remediations)

## Summary

| ID | Result | Class | Notes |
|---|---|---|---|
| SMK-01 | PASS | | Health JSON ok; panel login chrome; engine home; Mailpit UI |
| SMK-02 | PASS | | All four demo users land on calendar; headers match; CFO has no switch / New Reservation |
| AUTH-01 | PASS | | Deep link `/rms/reservations/bookings` survives sign-in as Mateo |
| AUTH-02 | PASS | | Same credentials message; 6th rapid attempt is rate-limited |
| AUTH-03 | PASS | SCENARIO | Reset + new password work. Form hides after first send — see notes |
| AUTH-04 | PASS | | Invitee accepts; header `TASK INVITEE — MANAGER`; history + `user.activated` |
| AUTH-05 | PASS | | Accept in Carolina’s session swaps to `SESSION SWAP — MANAGER` |
| AUTH-06 | PASS | | Disable Lucía → next nav is `/login?redirect=…`; login shows credentials message |
| AUTH-07 | PASS | | `//evil.com`, `/\evil.com`, `https://evil.com` all stay on calendar |
| AUTH-08 | PASS | | CFO / Mateo / Lucía redirects + forbidden toast + sidebar gating |
| USR-02 | PASS | | Own row is Edit + History only; role locked; name editable |
| ROLE-01 | FAIL | SCENARIO / harness | Toast `Roles updated` after grant. Role-column History + `/api/auth/me` HTML not asserted cleanly |
| RATE-01 | PASS | | Lucía: `VIEW ONLY — ADMIN / DIRECTOR EDITS RATES`; inputs disabled; no approval |
| RATE-03 | FAIL | harness | Rates page loaded (seeded 13300 / 25000 / 199500 visible). Price-check table was below the fold and not read |
| RATE-05 | FAIL | harness | Not completed (strict locator / publish bar) |
| RATE-06 | FAIL | harness | Not completed (same) |
| ENG-01 | PASS | | Mateo published copy with empty approval; `PUBLISHED — V2 · Mateo R.` |
| ENG-02 | FAIL | harness | Copy enabled + `ADMIN · MANAGER` pills seen. Rule-input disabled check inconclusive |
| ENG-05 | PASS | | Lucía: `VIEW ONLY — SALES EXEC`; every input disabled; no approval |
| BR-01 | PASS | | KPIs 45 / 20 / 15 / 6; pending + OPS-006 flagged |
| BR-02 | FAIL | harness | Reset-to-source returned `12`. Differ-at-15 and publish sentence not fully captured |
| BR-06 | FAIL | harness | Same URL bounce as AUTH-08; toast waiter lost the dismiss |
| USR-01 | — | not run | |
| USR-03 | — | not run | |
| ROLE-02 | — | not run | |
| ROLE-03 | — | not run | |
| ROLE-04 | — | not run | |
| ROLE-05 | — | not run | |
| RATE-02 | — | not run | |
| RATE-04 | — | not run | |
| ENG-03 | — | not run | |
| ENG-04 | — | not run | |
| BR-03 | — | not run | |
| BR-04 | — | not run | |
| BR-05 | — | not run | |
| VIS-01 | — | not run | P3 / on request |
| VIS-02 | — | not run | P3 / on request |

**Counts:** 16 passed · 6 failed (harness / incomplete, not product verdicts) · 16 not run

## Environment

- Memory available at `up.sh`: 6.76 GiB (warn threshold `E2E_MIN_MEM_GB=6`, fail below 3). No fail.
- `COMPOSE_PROJECT_NAME`: `anakata-e2e`
- First `up.sh`: kept existing `.env` (empty `APP_KEY` → `key:generate`). Copied `api.testing.env` and frontend `.env`. Staged compose (mysql/redis/mailpit `--wait`, then app, composer, reset, Nuxt preview on 3001/3000). Last line `ALL UP`.
- **ENV remediations (container only, not committed):** `/app/storage` and `/app/bootstrap/cache` were `root:root` 755. php-fpm / Horizon run as `application` (uid 1000). Blade compile of the reset-password mail failed (`tempnam(): file created in the system's temporary directory`); `laravel.log` could not be opened. `GET /` healthcheck was 500 (container `unhealthy`). `chown -R application:application /app/storage /app/bootstrap/cache` fixed it. Retried `ResetPasswordNotification` then arrived in Mailpit. Same class of fix as Sprint 1 REPORT (`storage/logs` owned by root).

## Scenarios

### SMK-01 · Stack is up

- **Result:** PASS
- E1 · `{"status":"ok","checks":{"db":"ok","redis":"ok","queue":"ok"}}`
- E2 · `/login`: ANAKATA, `RMS · REVENUE ENGINE`, Sign in, Email, Password, Forgot password?
- E3 · Engine home: ANAKATA, `INTIMATE YACHT EXPEDITIONS`, Expeditions / Private Charter, coords `0°40′S 90°33′W · Galápagos, Ecuador` (HTML), footer + `API · OK`
- E4 · Mailpit UI, `/livez` 200

### SMK-02 · Every demo user can sign in and out

- **Result:** PASS
- Carolina → calendar, `CAROLINA M. — ADMIN`, Permissions + Business Rules, RMS/CRM
- Mateo → calendar, `MATEO R. — MANAGER`, no Permissions / Business Rules
- Lucía → calendar, `LUCÍA B. — SALES EXEC`, same gating
- CFO → calendar, `CFO (EXTERNAL) — EXTERNAL FINANCE`, no switch, no New Reservation
- Sign-out → `/login`

### AUTH-01 · Return to the page you asked for

- **Result:** PASS
- `/rms/reservations/bookings` → `/login?redirect=/rms/reservations/bookings`
- Mateo lands on Bookings (`Coming in Sprint 4`), header `MATEO R. — MANAGER`

### AUTH-02 · Failed sign-ins look identical

- **Result:** PASS
- Wrong password and unknown email: `These credentials do not match our records.`
- 6th rapid attempt: `Too many attempts. Try again in a minute.`

### AUTH-03 · Forgot and reset a password

- **Result:** PASS
- **Class (note):** SCENARIO — after the first `Send reset link` the email field is removed (`v-if="!sent"`). The scenario asks for two submits on one page. Reloading `/forgot-password` is required to send the second address. Both notices were `If that email is on an account, we sent a reset link.`
- Mail subject `Reset your Anakata password`; URL `/reset-password?token=&email=`
- Reset hint `At least 8 characters.` → `/login?notice=password-changed` / `Password changed`
- Old `password` rejected; `Newpass1` → calendar, `CAROLINA M. — ADMIN`

### AUTH-04 · Accept an invitation

- **Result:** PASS
- Toast path + row pill `INVITED` → mail `Set your Anakata password`
- Accept: `Set your password` / `At least 8 characters.` → `TASK INVITEE — MANAGER` on calendar
- Carolina’s row `ACTIVE`. History: `Invitation accepted` then `Invited as Manager`. Zone `Galápagos time · UTC−6`
- `db-check` → `"user.activated"`

### AUTH-05 / AUTH-06 / AUTH-07 / AUTH-08

- **Result:** PASS (all)
- AUTH-05: same-session accept → `SESSION SWAP — MANAGER` on calendar
- AUTH-06: Disable Lucía → her next nav `/login?redirect=/rms/reservations/bookings`; login uses the credentials message
- AUTH-07: three open-redirect payloads stay on `http://localhost:3001/rms/reservations/calendar`
- AUTH-08: CFO no CRM; Mateo/Lucía bounced from Business Rules / Permissions with `You don't have permission to do that.`

### USR-02 · Your own row

- **Result:** PASS
- Carolina’s row actions: Edit, History — no Disable
- Edit modal: Role disabled, `You can't change your own role`; Name editable

### ENG-01 / ENG-05

- **Result:** PASS
- ENG-01: optional approval; Mateo published booking-note copy; state `● PUBLISHED — V2 · … · Mateo R.`
- ENG-05: `VIEW ONLY — SALES EXEC`; inputs disabled; no approval field

### BR-01 · Fresh-seed registry

- **Result:** PASS
- KPIs on screen: Rules tracked **45**, Adjusted here **20**, Set in other tabs **15**, Differ / flagged **6**
- Flagged set includes `TEXT IN DRAFTING`, `PENDING LEGAL`, `PENDING CLIENT`, OPS-006 `CONFIRMED`

### ROLE-01 / RATE-03 / RATE-05 / RATE-06 / ENG-02 / BR-02 / BR-06

- **Result:** FAIL
- **Class:** harness (agent automation), not a product verdict
- ROLE-01 E1 (toast `Roles updated`) passed. History sentence and Lucía `/api/auth/me` were not captured cleanly (role menu iteration; `/api/auth/me` returned HTML)
- RATE-03: base-rate grid showed seeded Suite `13300` / Owner `25000` / Charter `199500`. Price-check block was not scrolled into view
- RATE-05 / RATE-06: publish flow not finished
- ENG-02: copy textareas enabled and role pills present; disabled-state of fee/SLA inputs not proven
- BR-02: Reset restored `12`; differ flag + history sentence not proven
- BR-06: covered by AUTH-08; toast waiter lost the dismiss

## Notes for humans

1. Cloud `up.sh` does not chown bind-mounted `storage/`. Without `chown application:application storage bootstrap/cache`, Horizon cannot render mail views and `GET /` (compose healthcheck) is 500 even while `/api/health` is ok. Worth adding to `up.sh` (not done this run — observe-only).
2. AUTH-03: forgot-password UI is one-shot. Update the scenario to reload between the unknown and known emails, or keep the form visible under the notice.
3. Agent browser fonts are garbled; assertions used DOM / accessibility text, not OCR.
4. Visual P3 (VIS-01, VIS-02) and several P2 user/role/config scenarios were not run.
5. Application code, seeders and scenario files were not changed.
